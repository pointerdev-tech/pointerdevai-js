{
  "name": "ai-chat-js",
  "private": true,
  "version": "0.1.0",
  "description": "PointerDev AI chat JavaScript monorepo (client + widget).",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npm -w @pointerdevks/ai-chat-client run lint && npm -w @pointerdevks/ai-chat run lint",
    "build": "npm -w @pointerdevks/ai-chat run build",
    "smoke": "npm -w @pointerdevks/ai-chat run smoke",
    "test": "npm run smoke"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}